.Rproj.user
.Rhistory
.RData
.Ruserdata

# Google service account credentials — never commit these
.secrets/
*.json

# macOS + local artefacts
.DS_Store
*.zip

# Design handover files — kept locally, not versioned
design_handoff_cea_synthesis/

# shinyapps.io deployment metadata — recreated on deploy
rsconnect/
